module.exports = {
    // fails on chrome>=96
    'check region by element within shadow dom with vg': {skip: true},
    // the baselines for this test are ios13 which is very unstable on sauce now
    'appium iOS check window': {skip: true},
}
