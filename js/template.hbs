// {{test.name}}
{{#each commands.deps as |dep|}}
{{dep}}
{{/each}}

describe{{#if test.skip}}.skip{{/if}}('Coverage Tests - {{test.key}}', () => {
  {{#each commands.vars as |var|}}
  {{var}}
  {{/each}}

  beforeEach(async () => {
    {{#each commands.beforeEach as |command|}}
    {{command}}
    {{/each}}
  })

  afterEach(async () => {
    {{#each commands.afterEach as |command|}}
    {{command}}
    {{/each}}
  })

  it('{{test.name}}{{#tags test.tags}}@{{this}}{{/tags}}', async () => {
    {{#each commands.default as |command|}}
    {{command}}
    {{/each}}
  })
})