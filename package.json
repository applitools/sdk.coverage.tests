{
  "name": "@applitools/coverage-tests",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=10.0.0"
  },
  "main": "index.js",
  "description": "Coverage Tests for Applitools SDKs",
  "author": "Applitools <team@applitools.com>",
  "license": "SEE LICENSE IN LICENSE",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/applitools/sdk.coverage.tests.git"
  },
  "scripts": {
    "dotnet:generate": "npm install && cd dotNet && coverage-tests create-tests --path .",
    "dotnet:run:parallel": "cd ./dotNet/test && APPLITOOLS_BATCH_ID=$(uuidgen) dotnet test --logger \"junit;LogFilePath=./../coverage-test-report.xml\"",
    "dotnet:report": "cd dotNet && coverage-tests process-report --path .",
    "dotnet:tests:local": "npm run dotnet:generate && npm run dotnet:run:parallel ; npm run dotnet:report",
	"dotnet:tests": "./dotNet/dotnet_tests.sh"
  },
  "dependencies": {
    "@applitools/sdk-coverage-tests": "latest",
    "@typescript-eslint/parser": "^2.14.0",
    "typescript": "^3.7.4"
  },
  "devDependencies": {
    "mocha": "^8.0.1"
  }
}
