# {{name}}
# Generated by Ruby template
{{#each output.hooks.deps as |dep|}}
{{dep}}
{{/each}}

RSpec.describe 'Coverage Tests' do
    {{#each output.hooks.vars as |var|}}
    {{var}}
    {{/each}}
  before :example do
    {{#each output.hooks.beforeEach as |command|}}
    {{command}}
    {{/each}}
  end
  after :example do
    {{#each output.hooks.afterEach as |command|}}
    {{command}}
    {{/each}}
  end
  {{#if skip}}skip{{else}}it{{/if}} '{{key}}' do
    {{#each output.commands as |command|}}
    {{command}}
    {{/each}}
  end
end

